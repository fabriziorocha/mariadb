apiVersion: v1
kind: Namespace
metadata:
  name: mariadb
  # as-is: o namespace no cluster NÃO tem Pod Security Admission. A imagem
  # oficial do MariaDB roda como root no entrypoint (faz chown do datadir);
  # não passaria no perfil `restricted` sem ajustes. Mantido sem PSA para
  # refletir o estado real.
